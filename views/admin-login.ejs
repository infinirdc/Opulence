<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Opulence - Connexion Admin</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="login-body">
    <div class="login-container">
        <div class="login-card">
            <div class="login-header">
                <h1 class="login-logo">
                    <i class="fas fa-crown"></i>
                    Opulence
                </h1>
                <p class="login-subtitle">Espace Administrateur</p>
            </div>
            
            <form method="POST" action="/admin/login" class="login-form">
                <% if (error) { %>
                    <div class="alert alert-error">
                        <i class="fas fa-exclamation-circle"></i>
                        <%= error %>
                    </div>
                <% } %>
                
                <div class="form-group">
                    <label for="pin" class="form-label">
                        <i class="fas fa-lock"></i>
                        Code PIN d'accès
                    </label>
                    <input type="password" 
                           id="pin" 
                           name="pin" 
                           class="form-input"
                           placeholder="Entrez le code PIN"
                           required
                           autofocus>
                </div>
                
                <button type="submit" class="btn btn-login">
                    <i class="fas fa-sign-in-alt"></i>
                    Se connecter
                </button>
                
                <div class="login-footer">
                    <a href="/" class="back-link">
                        <i class="fas fa-arrow-left"></i>
                        Retour au site public
                    </a>
                    <p class="security-note">
                        <i class="fas fa-shield-alt"></i>
                        Accès réservé au personnel autorisé
                    </p>
                </div>
            </form>
        </div>
        
        <div class="login-info">
            <h3><i class="fas fa-info-circle"></i> Information</h3>
            <p>Cet espace est destiné à la gestion du restaurant :</p>
            <ul>
                <li><i class="fas fa-check"></i> Gestion des stocks d'ingrédients</li>
                <li><i class="fas fa-check"></i> Création et modification des plats</li>
                <li><i class="fas fa-check"></i> Suivi des commandes en temps réel</li>
                <li><i class="fas fa-check"></i> Alertes de stock critique</li>
            </ul>
        </div>
    </div>

    <script>
        // Focus sur le champ PIN
        document.getElementById('pin').focus();
        
        // Empêcher la soumission multiple
        document.querySelector('.login-form').addEventListener('submit', function(e) {
            const submitBtn = this.querySelector('.btn-login');
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Connexion...';
        });
    </script>
</body>
</html>